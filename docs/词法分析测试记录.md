# 词法分析测试记录

> 词法分析器：`C/src/lex/lexer.l`
> 可执行文件：`C/bin/scanner`
> 测试集：`Tests/Tests1/`（A-1~A-10, B-1~B-2, C-1~C-2, D-1~D-3, E1-1~E1-2, E2-1~E2-2, E3-1~E3-2）
> 生成时间：自动生成，重新运行 `bash batch_test_lex.sh` 可更新

---

## A-1

**输入文件**：`Tests/Tests1/inputs/A-1.cmm`
**期望输出**：`Tests/Tests1/expects/A-1.exp`（语法树/错误，供后续阶段参考）

### 输入代码

```cmm
int main() {
  int base = 100;
  int step = base $ 3;
  return step;
}
```

### 词法分析器输出

```
TYPE
ID: main
LP
RP
LC
TYPE
ID: base
ASSIGNOP
INT: 100
SEMI
TYPE
ID: step
ASSIGNOP
ID: base
Error type A at Line 3: Mysterious characters "$".
INT: 3
SEMI
RETURN
ID: step
SEMI
RC
```

---

## A-10

**输入文件**：`Tests/Tests1/inputs/A-10.cmm`
**期望输出**：`Tests/Tests1/expects/A-10.exp`（语法树/错误，供后续阶段参考）

### 输入代码

```cmm
int main() {
  int a;
  a = 1;
  a.;
  return 0;
}
```

### 词法分析器输出

```
TYPE
ID: main
LP
RP
LC
TYPE
ID: a
SEMI
ID: a
ASSIGNOP
INT: 1
SEMI
ID: a
DOT
SEMI
RETURN
INT: 0
SEMI
RC
```

---

## A-2

**输入文件**：`Tests/Tests1/inputs/A-2.cmm`
**期望输出**：`Tests/Tests1/expects/A-2.exp`（语法树/错误，供后续阶段参考）

### 输入代码

```cmm
int main() {
  int count = 10;
  int limit = 20;
  int 2x = count + limit;
  return 0;
}
```

### 词法分析器输出

```
TYPE
ID: main
LP
RP
LC
TYPE
ID: count
ASSIGNOP
INT: 10
SEMI
TYPE
ID: limit
ASSIGNOP
INT: 20
SEMI
TYPE
INT: 2
ID: x
ASSIGNOP
ID: count
PLUS
ID: limit
SEMI
RETURN
INT: 0
SEMI
RC
```

---

## A-3

**输入文件**：`Tests/Tests1/inputs/A-3.cmm`
**期望输出**：`Tests/Tests1/expects/A-3.exp`（语法树/错误，供后续阶段参考）

### 输入代码

```cmm
int clamp(int val) {
  if (val < 0) {
    return;
  }
  return val;
}

int main() {
  return clamp(5);
}
```

### 词法分析器输出

```
TYPE
ID: clamp
LP
TYPE
ID: val
RP
LC
IF
LP
ID: val
RELOP
INT: 0
RP
LC
RETURN
SEMI
RC
RETURN
ID: val
SEMI
RC
TYPE
ID: main
LP
RP
LC
RETURN
ID: clamp
LP
INT: 5
RP
SEMI
RC
```

---

## A-4

**输入文件**：`Tests/Tests1/inputs/A-4.cmm`
**期望输出**：`Tests/Tests1/expects/A-4.exp`（语法树/错误，供后续阶段参考）

### 输入代码

```cmm
int total;
float ratio = 1.5;

int main() {
  return 0;
}
```

### 词法分析器输出

```
TYPE
ID: total
SEMI
TYPE
ID: ratio
ASSIGNOP
FLOAT: 1.5
SEMI
TYPE
ID: main
LP
RP
LC
RETURN
INT: 0
SEMI
RC
```

---

## A-5

**输入文件**：`Tests/Tests1/inputs/A-5.cmm`
**期望输出**：`Tests/Tests1/expects/A-5.exp`（语法树/错误，供后续阶段参考）

### 输入代码

```cmm
int main() {
  float bad = 3.14.15;
  return 0;
}
```

### 词法分析器输出

```
TYPE
ID: main
LP
RP
LC
TYPE
ID: bad
ASSIGNOP
FLOAT: 3.14
DOT
INT: 15
SEMI
RETURN
INT: 0
SEMI
RC
```

---

## A-6

**输入文件**：`Tests/Tests1/inputs/A-6.cmm`
**期望输出**：`Tests/Tests1/expects/A-6.exp`（语法树/错误，供后续阶段参考）

### 输入代码

```cmm
int main() {
  int a = 1;
  int b = 2;
  int arr[1.5];
  return 0;
}
```

### 词法分析器输出

```
TYPE
ID: main
LP
RP
LC
TYPE
ID: a
ASSIGNOP
INT: 1
SEMI
TYPE
ID: b
ASSIGNOP
INT: 2
SEMI
TYPE
ID: arr
LB
FLOAT: 1.5
RB
SEMI
RETURN
INT: 0
SEMI
RC
```

---

## A-7

**输入文件**：`Tests/Tests1/inputs/A-7.cmm`
**期望输出**：`Tests/Tests1/expects/A-7.exp`（语法树/错误，供后续阶段参考）

### 输入代码

```cmm
int add(int x, int y) {
  return x + y;
}

int main() {
  int r = add(3,);
  return r;
}
```

### 词法分析器输出

```
TYPE
ID: add
LP
TYPE
ID: x
COMMA
TYPE
ID: y
RP
LC
RETURN
ID: x
PLUS
ID: y
SEMI
RC
TYPE
ID: main
LP
RP
LC
TYPE
ID: r
ASSIGNOP
ID: add
LP
INT: 3
COMMA
RP
SEMI
RETURN
ID: r
SEMI
RC
```

---

## A-8

**输入文件**：`Tests/Tests1/inputs/A-8.cmm`
**期望输出**：`Tests/Tests1/expects/A-8.exp`（语法树/错误，供后续阶段参考）

### 输入代码

```cmm
int run_count;

int update(int inc) {
  int base = 0;
  if (inc > 0) {
    base = inc;
  }
  run_count += base;
  return run_count;
}
```

### 词法分析器输出

```
TYPE
ID: run_count
SEMI
TYPE
ID: update
LP
TYPE
ID: inc
RP
LC
TYPE
ID: base
ASSIGNOP
INT: 0
SEMI
IF
LP
ID: inc
RELOP
INT: 0
RP
LC
ID: base
ASSIGNOP
ID: inc
SEMI
RC
ID: run_count
PLUS
ASSIGNOP
ID: base
SEMI
RETURN
ID: run_count
SEMI
RC
```

---

## A-9

**输入文件**：`Tests/Tests1/inputs/A-9.cmm`
**期望输出**：`Tests/Tests1/expects/A-9.exp`（语法树/错误，供后续阶段参考）

### 输入代码

```cmm
int main() {
  int x = 3;
  int y = 4;
  x = x + y;
  int z;
  return z;
}
```

### 词法分析器输出

```
TYPE
ID: main
LP
RP
LC
TYPE
ID: x
ASSIGNOP
INT: 3
SEMI
TYPE
ID: y
ASSIGNOP
INT: 4
SEMI
ID: x
ASSIGNOP
ID: x
PLUS
ID: y
SEMI
TYPE
ID: z
SEMI
RETURN
ID: z
SEMI
RC
```

---

## B-1

**输入文件**：`Tests/Tests1/inputs/B-1.cmm`
**期望输出**：`Tests/Tests1/expects/B-1.exp`（语法树/错误，供后续阶段参考）

### 输入代码

```cmm
struct Sensor {
  int id;
  float value;
  int active;
};

int readings[2.0];

int offset;

int process(int mode,) {
  struct { int tag; };
  int total = 0;
  float weights[4] = {0.25, 0.5, 1.0};
  struct Sensor s;
  s.id = mode;
  s.value = 0.0;
  s.active = 1;

  while (s.value > 0.0);

  while (s.value > 0.0) {
    s.value = s.value - 1.0;
    total = total + 1;
  }

  return total;
}
```

### 词法分析器输出

```
STRUCT
ID: Sensor
LC
TYPE
ID: id
SEMI
TYPE
ID: value
SEMI
TYPE
ID: active
SEMI
RC
SEMI
TYPE
ID: readings
LB
FLOAT: 2.0
RB
SEMI
TYPE
ID: offset
SEMI
TYPE
ID: process
LP
TYPE
ID: mode
COMMA
RP
LC
STRUCT
LC
TYPE
ID: tag
SEMI
RC
SEMI
TYPE
ID: total
ASSIGNOP
INT: 0
SEMI
TYPE
ID: weights
LB
INT: 4
RB
ASSIGNOP
LC
FLOAT: 0.25
COMMA
FLOAT: 0.5
COMMA
FLOAT: 1.0
RC
SEMI
STRUCT
ID: Sensor
ID: s
SEMI
ID: s
DOT
ID: id
ASSIGNOP
ID: mode
SEMI
ID: s
DOT
ID: value
ASSIGNOP
FLOAT: 0.0
SEMI
ID: s
DOT
ID: active
ASSIGNOP
INT: 1
SEMI
WHILE
LP
ID: s
DOT
ID: value
RELOP
FLOAT: 0.0
RP
SEMI
WHILE
LP
ID: s
DOT
ID: value
RELOP
FLOAT: 0.0
RP
LC
ID: s
DOT
ID: value
ASSIGNOP
ID: s
DOT
ID: value
MINUS
FLOAT: 1.0
SEMI
ID: total
ASSIGNOP
ID: total
PLUS
INT: 1
SEMI
RC
RETURN
ID: total
SEMI
RC
```

---

## B-2

**输入文件**：`Tests/Tests1/inputs/B-2.cmm`
**期望输出**：`Tests/Tests1/expects/B-2.exp`（语法树/错误，供后续阶段参考）

### 输入代码

```cmm
int data[5]];

int size;

int fill(int val) {
  int i = 0;
  while (i < 5) {
    data[i] = val;
    i = i + 1;
  }
  int result = 0;
  return result;
}

int process(int n) {
  float avg = 0.0;
  avg = avg + 1.0;
  int count;
  count = 0;
  while (avg > 0.0) {
    count = count + 1;
    avg = avg - 0.5;
  }
  int final;
  final = count;
  return final;
}
```

### 词法分析器输出

```
TYPE
ID: data
LB
INT: 5
RB
RB
SEMI
TYPE
ID: size
SEMI
TYPE
ID: fill
LP
TYPE
ID: val
RP
LC
TYPE
ID: i
ASSIGNOP
INT: 0
SEMI
WHILE
LP
ID: i
RELOP
INT: 5
RP
LC
ID: data
LB
ID: i
RB
ASSIGNOP
ID: val
SEMI
ID: i
ASSIGNOP
ID: i
PLUS
INT: 1
SEMI
RC
TYPE
ID: result
ASSIGNOP
INT: 0
SEMI
RETURN
ID: result
SEMI
RC
TYPE
ID: process
LP
TYPE
ID: n
RP
LC
TYPE
ID: avg
ASSIGNOP
FLOAT: 0.0
SEMI
ID: avg
ASSIGNOP
ID: avg
PLUS
FLOAT: 1.0
SEMI
TYPE
ID: count
SEMI
ID: count
ASSIGNOP
INT: 0
SEMI
WHILE
LP
ID: avg
RELOP
FLOAT: 0.0
RP
LC
ID: count
ASSIGNOP
ID: count
PLUS
INT: 1
SEMI
ID: avg
ASSIGNOP
ID: avg
MINUS
FLOAT: 0.5
SEMI
RC
TYPE
ID: final
SEMI
ID: final
ASSIGNOP
ID: count
SEMI
RETURN
ID: final
SEMI
RC
```

---

## C-1

**输入文件**：`Tests/Tests1/inputs/C-1.cmm`
**期望输出**：`Tests/Tests1/expects/C-1.exp`（语法树/错误，供后续阶段参考）

### 输入代码

```cmm
struct Stack {
  int data[10];
  int top;
};

int main() {
  struct Stack st;
  int i;
  int total;

  st.top = 0;
  i = 0;
  total = 0;

  while (i < 5) {
    st.data[st.top] = i * i;
    st.top = st.top + 1;
    i = i + 1;
  }

  while (st.top > 0) {
    st.top = st.top - 1;
    total = total + st.data[st.top];
  }

  if (total > 30) {
    return total - 30;
  }

  return total;
}
```

### 词法分析器输出

```
STRUCT
ID: Stack
LC
TYPE
ID: data
LB
INT: 10
RB
SEMI
TYPE
ID: top
SEMI
RC
SEMI
TYPE
ID: main
LP
RP
LC
STRUCT
ID: Stack
ID: st
SEMI
TYPE
ID: i
SEMI
TYPE
ID: total
SEMI
ID: st
DOT
ID: top
ASSIGNOP
INT: 0
SEMI
ID: i
ASSIGNOP
INT: 0
SEMI
ID: total
ASSIGNOP
INT: 0
SEMI
WHILE
LP
ID: i
RELOP
INT: 5
RP
LC
ID: st
DOT
ID: data
LB
ID: st
DOT
ID: top
RB
ASSIGNOP
ID: i
STAR
ID: i
SEMI
ID: st
DOT
ID: top
ASSIGNOP
ID: st
DOT
ID: top
PLUS
INT: 1
SEMI
ID: i
ASSIGNOP
ID: i
PLUS
INT: 1
SEMI
RC
WHILE
LP
ID: st
DOT
ID: top
RELOP
INT: 0
RP
LC
ID: st
DOT
ID: top
ASSIGNOP
ID: st
DOT
ID: top
MINUS
INT: 1
SEMI
ID: total
ASSIGNOP
ID: total
PLUS
ID: st
DOT
ID: data
LB
ID: st
DOT
ID: top
RB
SEMI
RC
IF
LP
ID: total
RELOP
INT: 30
RP
LC
RETURN
ID: total
MINUS
INT: 30
SEMI
RC
RETURN
ID: total
SEMI
RC
```

---

## C-2

**输入文件**：`Tests/Tests1/inputs/C-2.cmm`
**期望输出**：`Tests/Tests1/expects/C-2.exp`（语法树/错误，供后续阶段参考）

### 输入代码

```cmm
int fib[20];

int build_fib(int n) {
  int i;
  fib[0] = 0;
  fib[1] = 1;
  i = 2;
  while (i < n) {
    fib[i] = fib[i - 1] + fib[i - 2];
    i = i + 1;
  }
}

int sum_even(int n) {
  int i;
  int total;
  i = 0;
  total = 0;
  while (i < n) {
    if ((fib[i] - (fib[i] / 2) * 2) == 0) {
      total = total + fib[i];
    }
    i = i + 1;
  }
  return total;
}

int main() {
  int n;
  int result;
  n = 15;
  build_fib(n);
  result = sum_even(n);
  return result;
}
```

### 词法分析器输出

```
TYPE
ID: fib
LB
INT: 20
RB
SEMI
TYPE
ID: build_fib
LP
TYPE
ID: n
RP
LC
TYPE
ID: i
SEMI
ID: fib
LB
INT: 0
RB
ASSIGNOP
INT: 0
SEMI
ID: fib
LB
INT: 1
RB
ASSIGNOP
INT: 1
SEMI
ID: i
ASSIGNOP
INT: 2
SEMI
WHILE
LP
ID: i
RELOP
ID: n
RP
LC
ID: fib
LB
ID: i
RB
ASSIGNOP
ID: fib
LB
ID: i
MINUS
INT: 1
RB
PLUS
ID: fib
LB
ID: i
MINUS
INT: 2
RB
SEMI
ID: i
ASSIGNOP
ID: i
PLUS
INT: 1
SEMI
RC
RC
TYPE
ID: sum_even
LP
TYPE
ID: n
RP
LC
TYPE
ID: i
SEMI
TYPE
ID: total
SEMI
ID: i
ASSIGNOP
INT: 0
SEMI
ID: total
ASSIGNOP
INT: 0
SEMI
WHILE
LP
ID: i
RELOP
ID: n
RP
LC
IF
LP
LP
ID: fib
LB
ID: i
RB
MINUS
LP
ID: fib
LB
ID: i
RB
DIV
INT: 2
RP
STAR
INT: 2
RP
RELOP
INT: 0
RP
LC
ID: total
ASSIGNOP
ID: total
PLUS
ID: fib
LB
ID: i
RB
SEMI
RC
ID: i
ASSIGNOP
ID: i
PLUS
INT: 1
SEMI
RC
RETURN
ID: total
SEMI
RC
TYPE
ID: main
LP
RP
LC
TYPE
ID: n
SEMI
TYPE
ID: result
SEMI
ID: n
ASSIGNOP
INT: 15
SEMI
ID: build_fib
LP
ID: n
RP
SEMI
ID: result
ASSIGNOP
ID: sum_even
LP
ID: n
RP
SEMI
RETURN
ID: result
SEMI
RC
```

---

## D-1

**输入文件**：`Tests/Tests1/inputs/D-1.cmm`
**期望输出**：`Tests/Tests1/expects/D-1.exp`（语法树/错误，供后续阶段参考）

### 输入代码

```cmm
int main() {
  int a = 0xFF00;
  int b = 0x0F0F;
  int c = 0644;
  int d = 0x3A;
  return 0;
}
```

### 词法分析器输出

```
TYPE
ID: main
LP
RP
LC
TYPE
ID: a
ASSIGNOP
INT: 65280
SEMI
TYPE
ID: b
ASSIGNOP
INT: 3855
SEMI
TYPE
ID: c
ASSIGNOP
INT: 420
SEMI
TYPE
ID: d
ASSIGNOP
INT: 58
SEMI
RETURN
INT: 0
SEMI
RC
```

---

## D-2

**输入文件**：`Tests/Tests1/inputs/D-2.cmm`
**期望输出**：`Tests/Tests1/expects/D-2.exp`（语法树/错误，供后续阶段参考）

### 输入代码

```cmm
int main() {
  float p = 3.14e0;
  float q = 6.02e23;
  float r = 1.38e-2;
  float s = 2.72E1;
  return 0;
}
```

### 词法分析器输出

```
TYPE
ID: main
LP
RP
LC
TYPE
ID: p
ASSIGNOP
FLOAT: 3.14e0
SEMI
TYPE
ID: q
ASSIGNOP
FLOAT: 6.02e23
SEMI
TYPE
ID: r
ASSIGNOP
FLOAT: 1.38e-2
SEMI
TYPE
ID: s
ASSIGNOP
FLOAT: 2.72E1
SEMI
RETURN
INT: 0
SEMI
RC
```

---

## D-3

**输入文件**：`Tests/Tests1/inputs/D-3.cmm`
**期望输出**：`Tests/Tests1/expects/D-3.exp`（语法树/错误，供后续阶段参考）

### 输入代码

```cmm
int main() {
  int x /* counter */ = 0;
  x = x + 1;
  if /* check */ (x > 0) {
    return x;
  }
  return 0;
}
```

### 词法分析器输出

```
TYPE
ID: main
LP
RP
LC
TYPE
ID: x
ASSIGNOP
INT: 0
SEMI
ID: x
ASSIGNOP
ID: x
PLUS
INT: 1
SEMI
IF
LP
ID: x
RELOP
INT: 0
RP
LC
RETURN
ID: x
SEMI
RC
RETURN
INT: 0
SEMI
RC
```

---

## E1-1

**输入文件**：`Tests/Tests1/inputs/E1-1.cmm`
**期望输出**：`Tests/Tests1/expects/E1-1.exp`（语法树/错误，供后续阶段参考）

### 输入代码

```cmm
int check_range(int addr) {
  int lo = 0x0000;
  int hi = 0xFFFF;
  if (addr >= lo) {
    if (addr <= hi) {
      return 0755;
    }
  }
  return 0x00;
}

int main() {
  int base = 0x1A00;
  int code = check_range(base);
  return code;
}
```

### 词法分析器输出

```
TYPE
ID: check_range
LP
TYPE
ID: addr
RP
LC
TYPE
ID: lo
ASSIGNOP
INT: 0
SEMI
TYPE
ID: hi
ASSIGNOP
INT: 65535
SEMI
IF
LP
ID: addr
RELOP
ID: lo
RP
LC
IF
LP
ID: addr
RELOP
ID: hi
RP
LC
RETURN
INT: 493
SEMI
RC
RC
RETURN
INT: 0
SEMI
RC
TYPE
ID: main
LP
RP
LC
TYPE
ID: base
ASSIGNOP
INT: 6656
SEMI
TYPE
ID: code
ASSIGNOP
ID: check_range
LP
ID: base
RP
SEMI
RETURN
ID: code
SEMI
RC
```

---

## E1-2

**输入文件**：`Tests/Tests1/inputs/E1-2.cmm`
**期望输出**：无

### 输入代码

```cmm
int main() {
  int a = 0;
  int b = 0xQQQ;
  int c = 099;
  return a + b + c;
}
```

### 词法分析器输出

```
TYPE
ID: main
LP
RP
LC
TYPE
ID: a
ASSIGNOP
INT: 0
SEMI
TYPE
ID: b
ASSIGNOP
Error type A at Line 3: Illegal hexadecimal number "0xQQQ".
SEMI
TYPE
ID: c
ASSIGNOP
Error type A at Line 4: Illegal octal number "099".
SEMI
RETURN
ID: a
PLUS
ID: b
PLUS
ID: c
SEMI
RC
```

---

## E2-1

**输入文件**：`Tests/Tests1/inputs/E2-1.cmm`
**期望输出**：`Tests/Tests1/expects/E2-1.exp`（语法树/错误，供后续阶段参考）

### 输入代码

```cmm
int main() {
  float mass = 9.1e-2;
  float speed = 2.99e8;
  float gravity = 9.8E0;
  float energy = 1.23e-4;
  float result;
  result = mass + speed + gravity + energy;
  return 0;
}
```

### 词法分析器输出

```
TYPE
ID: main
LP
RP
LC
TYPE
ID: mass
ASSIGNOP
FLOAT: 9.1e-2
SEMI
TYPE
ID: speed
ASSIGNOP
FLOAT: 2.99e8
SEMI
TYPE
ID: gravity
ASSIGNOP
FLOAT: 9.8E0
SEMI
TYPE
ID: energy
ASSIGNOP
FLOAT: 1.23e-4
SEMI
TYPE
ID: result
SEMI
ID: result
ASSIGNOP
ID: mass
PLUS
ID: speed
PLUS
ID: gravity
PLUS
ID: energy
SEMI
RETURN
INT: 0
SEMI
RC
```

---

## E2-2

**输入文件**：`Tests/Tests1/inputs/E2-2.cmm`
**期望输出**：无

### 输入代码

```cmm
int main() {
  float a = 0.0;
  float b = 2.5e;
  float c = 1.0E+;
  return 0;
}
```

### 词法分析器输出

```
TYPE
ID: main
LP
RP
LC
TYPE
ID: a
ASSIGNOP
FLOAT: 0.0
SEMI
TYPE
ID: b
ASSIGNOP
Error type A at Line 3: Illegal floating point number "2.5e".
SEMI
TYPE
ID: c
ASSIGNOP
Error type A at Line 4: Illegal floating point number "1.0E".
PLUS
SEMI
RETURN
INT: 0
SEMI
RC
```

---

## E3-1

**输入文件**：`Tests/Tests1/inputs/E3-1.cmm`
**期望输出**：`Tests/Tests1/expects/E3-1.exp`（语法树/错误，供后续阶段参考）

### 输入代码

```cmm
/* Matrix utility: supports init and trace */
/* All values default to zero on init */

int mat[3][3];

/* Set all elements to val */
int init_mat(int val) {
  int i;
  int j;
  i = 0;
  while (i < 3) {
    j = 0;
    while (j < 3) {
      mat[i][j] = val; /* assign element */
      j = j + 1;
    }
    i = i + 1;
  }
}

int main() {
  init_mat(0);         /* zero out matrix */
  mat[1][1] = 1;       /* set diagonal */
  return mat[1][1];    // return center value
}
```

### 词法分析器输出

```
TYPE
ID: mat
LB
INT: 3
RB
LB
INT: 3
RB
SEMI
TYPE
ID: init_mat
LP
TYPE
ID: val
RP
LC
TYPE
ID: i
SEMI
TYPE
ID: j
SEMI
ID: i
ASSIGNOP
INT: 0
SEMI
WHILE
LP
ID: i
RELOP
INT: 3
RP
LC
ID: j
ASSIGNOP
INT: 0
SEMI
WHILE
LP
ID: j
RELOP
INT: 3
RP
LC
ID: mat
LB
ID: i
RB
LB
ID: j
RB
ASSIGNOP
ID: val
SEMI
ID: j
ASSIGNOP
ID: j
PLUS
INT: 1
SEMI
RC
ID: i
ASSIGNOP
ID: i
PLUS
INT: 1
SEMI
RC
RC
TYPE
ID: main
LP
RP
LC
ID: init_mat
LP
INT: 0
RP
SEMI
ID: mat
LB
INT: 1
RB
LB
INT: 1
RB
ASSIGNOP
INT: 1
SEMI
RETURN
ID: mat
LB
INT: 1
RB
LB
INT: 1
RB
SEMI
RC
```

---

## E3-2

**输入文件**：`Tests/Tests1/inputs/E3-2.cmm`
**期望输出**：无

### 输入代码

```cmm
/*/* 嵌套注释不被支持，此处为错误示例 */*/

int main() {
  int x = 1;
  return x;
}
```

### 词法分析器输出

```
STAR
DIV
TYPE
ID: main
LP
RP
LC
TYPE
ID: x
ASSIGNOP
INT: 1
SEMI
RETURN
ID: x
SEMI
RC
```

---

